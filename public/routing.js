/*const routes = {
  '/':      () => import('./oldalak/home.js'),
  '/register': () => import('./oldalak/reg-log.js'),
};

export async function handleRoute(path) {
  const load = routes[path] || routes['/'];
  const module = await load();
  module.render();
}

*/